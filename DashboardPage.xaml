<UserControl x:Class="WorkPartner.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkPartner"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Loaded="Window_Loaded" IsVisibleChanged="DashboardPage_IsVisibleChanged">
    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        </Style>
        <Style x:Key="SubtleButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#EFEFEF"/>
            <Setter Property="Foreground" Value="#333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DDDDDD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <DockPanel Grid.Column="0" Margin="10">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
                <TextBlock Text="💰" FontSize="20" VerticalAlignment="Center"/>
                <TextBlock x:Name="CoinDisplayTextBlock" Text="0" FontSize="18" VerticalAlignment="Center" Margin="5,0,20,0" Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBlock x:Name="CurrentTaskDisplay" Text="선택된 과목 없음" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBlock x:Name="ActiveProcessDisplay" Text="활성 프로그램: -" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>

            <Grid DockPanel.Dock="Top" Margin="0,20">
                <TextBlock x:Name="MainTimeDisplay" Text="00:00:00" FontSize="80" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource PrimaryTextBrush}"/>
                <TextBlock x:Name="SelectedTaskTotalTimeDisplay" Text="선택 과목 총계: 00:00:00" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5" Foreground="{StaticResource SecondaryTextBrush}"/>
            </Grid>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="타임라인" Style="{StaticResource HeaderTextStyle}"/>
                    <!-- [수정] Height 값을 350으로 변경 -->
                    <ScrollViewer Height="350" VerticalScrollBarVisibility="Auto">
                        <Canvas x:Name="TimeTableCanvas" Height="1440"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button Content="수동 기록 추가" Click="AddManualLogButton_Click" Margin="5" Style="{StaticResource SubtleButton}"/>
                <Button Content="메모장" Click="MemoButton_Click" Margin="5" Style="{StaticResource SubtleButton}"/>
                <Button x:Name="BgmPlayButton" Content="백색 소음 재생" Click="BgmPlayButton_Click" Margin="5" Style="{StaticResource SubtleButton}"/>
            </StackPanel>

            <Grid>
                <!-- 캐릭터 표시는 공간이 부족하므로 잠시 주석 처리하거나 다른 위치로 옮기는 것을 고려할 수 있습니다. -->
                <!-- <local:CharacterDisplay x:Name="DashboardCharacterDisplay" Grid.Row="0" Margin="10"/> -->

                <StackPanel x:Name="SessionReviewPanel" Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="#FFF0F8FF" Margin="10" Visibility="Collapsed">
                    <TextBlock Text="방금 세션의 집중도는 어땠나요?" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="★" Tag="1" Click="RateSessionButton_Click" Style="{StaticResource RatingButton}"/>
                        <Button Content="★" Tag="2" Click="RateSessionButton_Click" Style="{StaticResource RatingButton}"/>
                        <Button Content="★" Tag="3" Click="RateSessionButton_Click" Style="{StaticResource RatingButton}"/>
                        <Button Content="★" Tag="4" Click="RateSessionButton_Click" Style="{StaticResource RatingButton}"/>
                        <Button Content="★" Tag="5" Click="RateSessionButton_Click" Style="{StaticResource RatingButton}"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Name="AiSuggestionTextBlock" Grid.Row="1" Text="" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Top" FontStyle="Italic" Foreground="DodgerBlue"/>
            </Grid>

        </DockPanel>

        <!-- Right Panel (이전과 동일) -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="10">
            <StackPanel>
                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="과목 관리" Style="{StaticResource HeaderTextStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TaskInput" KeyDown="TaskInput_KeyDown"/>
                            <Button Grid.Column="1" Content="추가" Click="AddTaskButton_Click" Margin="10,0,0,0"/>
                        </Grid>
                        <ListBox x:Name="TaskListBox" ItemsSource="{Binding TaskItems}" SelectionChanged="TaskListBox_SelectionChanged" DisplayMemberPath="Text" Margin="0,15,0,0" MaxHeight="150" BorderThickness="0"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="오늘의 할 일 (To-do List)" Style="{StaticResource HeaderTextStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TodoInput" KeyDown="TodoInput_KeyDown"/>
                            <Button Grid.Column="1" Content="추가" Click="AddTodoButton_Click" Margin="10,0,0,0"/>
                        </Grid>
                        <TreeView x:Name="TodoTreeView" ItemsSource="{Binding TodoItems}" Margin="0,15,0,0" MaxHeight="250" BorderThickness="0">
                            <!-- TreeView 템플릿은 이전과 동일 -->
                        </TreeView>
                        <ItemsControl x:Name="SuggestedTagsItemsControl" ItemsSource="{Binding SuggestedTags}" Margin="0,5,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}" Click="SuggestedTag_Click" Margin="2" FontSize="10" Style="{StaticResource SubtleButton}" Padding="6,3"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>