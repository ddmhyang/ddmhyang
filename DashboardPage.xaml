<UserControl x:Class="WorkPartner.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WorkPartner"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="Window_Loaded"
             IsVisibleChanged="DashboardPage_IsVisibleChanged">

    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <DockPanel Grid.Column="0" Margin="10,10,5,10">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="💰" FontSize="20" VerticalAlignment="Center"/>
                <TextBlock x:Name="CoinDisplayTextBlock" Text="0" FontSize="18" VerticalAlignment="Center" Margin="5,0,20,0"/>
                <TextBlock x:Name="CurrentTaskDisplay" Text="선택된 과목 없음" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="ActiveProcessDisplay" Text="활성 프로그램: -" FontSize="12" Foreground="Gray" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>

            <Grid DockPanel.Dock="Top">
                <TextBlock x:Name="MainTimeDisplay" Text="00:00:00" FontSize="72" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock x:Name="SelectedTaskTotalTimeDisplay" Text="선택 과목 총계: 00:00:00" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
            </Grid>

            <Border Style="{StaticResource PanelBorderStyle}" Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="타임라인" Style="{StaticResource HeaderTextStyle}"/>
                    <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto">
                        <Canvas x:Name="TimeTableCanvas" Height="1440"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="수동 기록 추가" Click="AddManualLogButton_Click" Margin="5"/>
                <Button Content="메모장" Click="MemoButton_Click" Margin="5"/>
                <Button x:Name="BgmPlayButton" Content="백색 소음 재생" Click="BgmPlayButton_Click" Margin="5"/>
            </StackPanel>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <local:CharacterDisplay x:Name="DashboardCharacterDisplay" Grid.Row="0" Margin="10"/>

                <StackPanel x:Name="SessionReviewPanel" Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="#FFF0F8FF" Margin="10" Visibility="Collapsed">
                    <TextBlock Text="방금 세션의 집중도는 어땠나요?" FontWeight="Bold" HorizontalAlignment="Center" Margin="5"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="★" Tag="1" Click="RateSessionButton_Click"/>
                        <Button Content="★" Tag="2" Click="RateSessionButton_Click"/>
                        <Button Content="★" Tag="3" Click="RateSessionButton_Click"/>
                        <Button Content="★" Tag="4" Click="RateSessionButton_Click"/>
                        <Button Content="★" Tag="5" Click="RateSessionButton_Click"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Name="AiSuggestionTextBlock" Grid.Row="1" Text="" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Top" FontStyle="Italic" Foreground="DodgerBlue"/>
            </Grid>
        </DockPanel>

        <!-- Right Panel -->
        <Grid Grid.Column="1" Margin="5,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PanelBorderStyle}">
                <StackPanel>
                    <TextBlock Text="과목 관리" Style="{StaticResource HeaderTextStyle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TaskInput" KeyDown="TaskInput_KeyDown"/>
                        <Button Grid.Column="1" Content="추가" Click="AddTaskButton_Click" Margin="5,0,0,0"/>
                    </Grid>
                    <ListBox x:Name="TaskListBox" ItemsSource="{Binding TaskItems}" SelectionChanged="TaskListBox_SelectionChanged" DisplayMemberPath="Text" Margin="0,10,0,0" MaxHeight="150">
                        <ListBox.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="삭제" Click="DeleteTaskButton_Click"/>
                            </ContextMenu>
                        </ListBox.ContextMenu>
                    </ListBox>
                </StackPanel>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,15,0,0">
                <Border Style="{StaticResource PanelBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="오늘의 할 일 (To-do List)" Style="{StaticResource HeaderTextStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TodoInput" KeyDown="TodoInput_KeyDown"/>
                            <Button Grid.Column="1" Content="추가" Click="AddTodoButton_Click" Margin="5,0,0,0"/>
                        </Grid>
                        <TreeView x:Name="TodoTreeView" ItemsSource="{Binding TodoItems}" Margin="0,10,0,0" MaxHeight="250">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding SubTasks}">
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}" Click="SaveTodos_Event"/>
                                        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" KeyDown="TodoTextBox_KeyDown"/>
                                        <ItemsControl ItemsSource="{Binding Tags}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="LightBlue" CornerRadius="5" Margin="2,0">
                                                        <TextBlock Text="{Binding}" Margin="3,1" FontSize="10"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                    <HierarchicalDataTemplate.ItemContainerStyle>
                                        <Style TargetType="TreeViewItem">
                                            <Setter Property="ContextMenu">
                                                <Setter.Value>
                                                    <ContextMenu>
                                                        <MenuItem Header="하위 작업 추가" Click="AddSubTaskMenuItem_Click"/>
                                                        <MenuItem Header="태그 추가" Click="AddTagMenuItem_Click"/>
                                                        <MenuItem Header="삭제" Click="DeleteTodoMenuItem_Click"/>
                                                    </ContextMenu>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </HierarchicalDataTemplate.ItemContainerStyle>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                        <ItemsControl x:Name="SuggestedTagsItemsControl" ItemsSource="{Binding SuggestedTags}" Margin="0,5,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}" Click="SuggestedTag_Click" Margin="2" FontSize="10" Background="LightGreen"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
