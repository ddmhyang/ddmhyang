<Window x:Class="WorkPartner.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkPartner"
        mc:Ignorable="d"
        Title="Color Picker" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Hue:" VerticalAlignment="Center"/>
        <Slider Grid.Row="0" Grid.Column="1" x:Name="HueSlider" Minimum="0" Maximum="360" ValueChanged="Slider_ValueChanged" Margin="10,0"/>
        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Value, ElementName=HueSlider, StringFormat={}{0:F0}}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Saturation:" VerticalAlignment="Center"/>
        <Slider Grid.Row="1" Grid.Column="1" x:Name="SaturationSlider" Minimum="0" Maximum="1" SmallChange="0.01" Value="1" ValueChanged="Slider_ValueChanged" Margin="10,0"/>
        <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding Value, ElementName=SaturationSlider, StringFormat={}{0:F2}}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Lightness:" VerticalAlignment="Center"/>
        <Slider Grid.Row="2" Grid.Column="1" x:Name="LightnessSlider" Minimum="0" Maximum="1" SmallChange="0.01" Value="0.5" ValueChanged="Slider_ValueChanged" Margin="10,0"/>
        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Value, ElementName=LightnessSlider, StringFormat={}{0:F2}}"/>

        <Rectangle Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" x:Name="ColorPreview" Height="50" Margin="0,10" Stroke="Black" StrokeThickness="1"/>

        <StackPanel Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="75" IsCancel="True" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
